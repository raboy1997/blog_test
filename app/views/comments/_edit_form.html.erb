
<%= form_for  @comment, url: update_comment_path(comment_id: @comment.id, post_id: @comment.post_id)  do |f| %>

    <div>
      <%= f.text_field :body %>
    </div>
    <div>
      <%= f.submit 'Update' %>
    </div>
<% end %>